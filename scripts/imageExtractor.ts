export async function extractImagesFromPdf(filePath: string) {
  void filePath;
  // Placeholder: wire pdfjs-dist operator list and image extraction here.
  return [] as Array<{ page: number; imagePath: string }>;
}
